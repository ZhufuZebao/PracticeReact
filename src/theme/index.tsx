import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#744DFE",
    },
    secondary: {
      main: "#744DFE",
    },
  },
});

export default theme;